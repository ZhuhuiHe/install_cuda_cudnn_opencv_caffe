1.the sequence to running
---install install_nvidia_driver.sh(if didn't install nvidia driver)
---install_opencv.sh (in the same folder include 2.4.13.zip )
   #返回版本数即安装成功
---install_cuda.sh (in the same folder include cuda_9.0.176_384.81_linux.run)
   # noice :the second choice is n,other is yes .
   # according your cuda, change the path
---cudnn_install.sh
---install_caffe.sh(you have to open the script file to read because there is important info )
   #in the same folder include caffe
